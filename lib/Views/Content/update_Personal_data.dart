import 'package:flutter/material.dart';

class updatePersonaldata extends StatelessWidget {
  const updatePersonaldata({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('update Personal data')),);
  }
}

import 'package:flutter/material.dart';

class goldenBadge extends StatelessWidget {
  const goldenBadge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('goldenBadge')),);
  }
}

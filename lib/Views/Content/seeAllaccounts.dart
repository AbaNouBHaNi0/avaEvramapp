import 'package:flutter/material.dart';

class seeAlldata extends StatelessWidget {
  const seeAlldata({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("seeAlldata")),
    );
  }
}
